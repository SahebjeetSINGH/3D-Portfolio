import React from "react";
import { link } from "react-router-dom";

import { styles } from "../styles";
import { navlinks } from "../con";
import { logo, menu, close } from "../assets";

const Navbar = () => {
  return <div>Navbar</div>;
};

export default Navbar;
